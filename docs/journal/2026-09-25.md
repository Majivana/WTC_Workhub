# Development Journal — 2026-09-25

## Objectives

- Consolidate release documentation and perform final local acceptance checks.
- Record database and AWS status without presenting plans as deployed infrastructure.
- Prepare an API demo script and gather architecture/health evidence.

## Work completed

- Audited the root README and found wiki status pages that still called implemented authentication,
  attendance, submission, verification, and reporting features planned; updated those pages.
- Added a release checklist, a seven-minute local API demo runbook, and an application architecture
  SVG that separates verified local components from planned AWS services.
- Added ADRs for the modular monolith, authentication/authorization, first-party attendance,
  versioned evidence/verification, and weekly reminders.
- Documented that the public demo has not been recorded or uploaded; no YouTube link or release tag
  was created.
- Captured the PostgreSQL 16 schema compatibility failure at the SQLite `PRAGMA` statement and
  removed the disposable local test container.

## Validation

Record the final Maven and Docker commands and outcomes in `docs/release/README.md` after the
release validation run. A successful local validation is not evidence of AWS deployment.

## Open items

- Record and review a 5–10 minute demonstration, then upload it to the approved YouTube account and
  add the real link.
- Remove remaining development identity request fields and compatibility behavior.
- Approve evidence/selfie retention and deletion rules.
- Implement and validate PostgreSQL before selecting RDS or Aurora.
- Deploy and test AWS components only after an account and budget are approved.
