# Evidence Management

Evidence is a first-class concept. Files are private objects and the database stores metadata:
filename, object key, MIME type, size, checksum, uploader, timestamp, and status.

Evidence access requires authorization. Browser-provided filenames are not trusted.
