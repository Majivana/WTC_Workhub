# Escalation Management

Supervisors can create escalations for repeated shortfalls, missing evidence, attendance
exceptions, rejected submissions, or support needs.

Statuses are `OPEN`, `IN_PROGRESS`, `RESOLVED`, and `CLOSED`. Severity is `LOW`, `MEDIUM`,
`HIGH`, or `CRITICAL`.
