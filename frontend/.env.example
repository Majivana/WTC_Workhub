# Public browser configuration only. Never put credentials or secrets in VITE_* variables.
VITE_API_BASE_URL=/api
# Optional local proxy target (server-side Vite configuration; not exposed to browser code).
WORKHUB_API_TARGET=http://localhost:8080
